---
import { ClientRouter } from "astro:transitions";
import Header from "../components/Header/Header.astro";
import Footer from "../components/Footer/Footer.astro";
const { day, hideUserAvatar } = Astro.props;

// Leer parámetro 'user' de la URL y el pathname
let userFromParam = undefined;
let pathname = undefined;
if (typeof Astro !== 'undefined' && Astro.url) {
  userFromParam = Astro.url.searchParams.get('user');
  pathname = Astro.url.pathname;
}
---

<!doctype html>
<html lang="en" data-base={import.meta.env.BASE_URL}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>Astro Basics</title>
    <ClientRouter />
  </head>
  <body>
    <Header day={day} hideUserAvatar={hideUserAvatar} />
    <slot />
    <Footer day={day} />
    
  </body>
</html>

<style>
  :global(*),
  :global(*::before),
  :global(*::after) {
    box-sizing: border-box;
  }

  :global(:root) {
    interpolate-size: allow-keywords;
  }


  html {
    min-width: min-content;
  }
  body {
    box-sizing: border-box;
    background-color: #111;
  }
</style>
