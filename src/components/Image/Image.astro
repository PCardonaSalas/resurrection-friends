---
const { src, alt, ...props } = Astro.props;
let base = import.meta.env.BASE_URL;
if (base.endsWith('/') && base.length > 1) base = base.slice(0, -1);
const imagePath = `${base}${src.startsWith('/') ? src : `/${src}`}`;
---

<img src={imagePath} alt={alt} {...props} />
